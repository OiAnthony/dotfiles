# Keep this file cheap: it runs for every Zsh process.
typeset -U path PATH
path=(
  "$HOME/.local/bin"
  "$HOME/.local/share/mise/shims"
  "$HOME/.local/share/pnpm/bin"
  "$HOME/.local/share/pnpm"
  "$HOME/Library/pnpm"
  "$HOME/.volta/bin"
  "$HOME/.cargo/bin"
  $path
)
export PATH
